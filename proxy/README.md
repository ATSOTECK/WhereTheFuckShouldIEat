To run the proxy server make sure to create a .env file to include the Google API key in the form of API_KEY=theAPIKey

Once the .env file is made run the following code to run the proxy server:

npm install

node proxy.js
